# apps/web — ownership externo

Este dashboard lo mantiene **otra persona del equipo**.

- Contrato HTTP: [`docs/context/api.md`](../../docs/context/api.md)
- Spec de pantallas: `docs/context/architecture.md` (sección Dashboard)
- El código aquí es un **stub de referencia** para probar la API en local; no es la UI de producción del frontend owner.

Backend / agente: `apps/api` + `packages/*`.
