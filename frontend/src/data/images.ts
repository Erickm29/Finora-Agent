/**
 * Legacy remote Stitch assets. Brand UI now uses local SVG components under
 * `src/assets/logo` and `src/assets/illustrations`. Keep this file only if a
 * residual consumer still references a key; prefer SVG components for new work.
 */
export const images = {
  logo: '', // replaced by FinoraLogo / FinoraMark
} as const
